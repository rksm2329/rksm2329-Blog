<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>高精度</title>
    <url>/2024/02/10/bigint/</url>
    <content><![CDATA[高精度运算对话框dalao：高精度这么《简单》的东西怎么还要讲呢？！！
我：巨佬%%%
加法先来想一下竖式加法。
竖式中，是每一位逐一相加，如果有进位，就标记一下。
那么高精度可以采用类似的方法。
循环遍历每一位，逐一相加，再处理进位。
减法跟加法类似。
乘法在竖式乘法中，是每一位逐位相乘，最后相加，所以我们可以利用一下。
除法在除法竖式中，是每一位除以除数，然后拿余数接上后面那个数再来除以除数。
但是，高精除高精里，没办法逐位相除，就可以一直减，减到没有就停下，然后记录次数。
取模差点忘了。。。
取模就是被除数 - 被除数 / 除数 * 除数。
压位你在想什么？压位？不可能！

所以现在你已经知道了怎么实现，可以开始写了。
但是，仅限于十进制的算法怎么够呢？当然要加上其他进制！
实现#include &lt;iostream&gt;#include &lt;algorithm&gt;namespace Integer &#123;const std::string symbol = &quot;0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ&quot;;template &lt;size_t kMaxLength, int binary = 10, class T = int&gt; class BigInt &#123;private:  T n, a[kMaxLength];  bool f;public:  BigInt() &#123; Init(); &#125;  T &amp;operator[](int i) &#123; return a[i]; &#125;  void Init() &#123;    n = f = 1;    std::fill(a, a + kMaxLength, 0);  &#125;  size_t size() &#123; return n; &#125;  bool empty() &#123; return !n; &#125;  friend std::istream &amp;operator&gt;&gt;(std::istream &amp;is, BigInt &amp;a) &#123;    std::string s;    is &gt;&gt; s;    a.n = s.size();    if (s[0] == &#x27;-&#x27;) &#123;      a.f = 0;      a.n--, s.erase(0, 1);    &#125;    for (int i = 0; i &lt; a.n; i++) &#123;      a[i] = (s[a.n - i - 1] &gt;= &#x27;A&#x27; &amp;&amp; s[a.n - i - 1] &lt;= &#x27;Z&#x27;                  ? s[a.n - i - 1] - &#x27;A&#x27; + 10                  : s[a.n - i - 1] - &#x27;0&#x27;);    &#125;    return is;  &#125;  friend std::ostream &amp;operator&lt;&lt;(std::ostream &amp;os, BigInt a) &#123;    for (; a.n &gt; 1 &amp;&amp; !a[a.n - 1]; a.n--) &#123;    &#125;    if (!a.f) &#123;      os &lt;&lt; &#x27;-&#x27;;    &#125;    for (int i = a.n - 1; i &gt;= 0; i--) &#123;      os &lt;&lt; symbol[a[i]];    &#125;    return os;  &#125;  void operator=(int x) &#123;    Init();    if (!x) &#123;      return;    &#125;    if (x &lt; 0) &#123;      x = -x, f = 0;    &#125;    n = 0;    while (x) &#123;      a[n++] = x % binary;      x /= binary;    &#125;  &#125;  void operator=(std::string x) &#123;    Init();    int st = 0;    if (x[0] == &#x27;-&#x27;) &#123;      f = 0, st++;    &#125;    n = 0;    int len = x.size();    for (int i = st; i &lt; len; i++) &#123;      a[n++] = (x[len - i - 1] &gt;= &#x27;A&#x27; &amp;&amp; x[len - i - 1] &lt;= &#x27;Z&#x27; ? x[len - i - 1] - &#x27;A&#x27; + 10 : x[len - i - 1] - &#x27;0&#x27;);    &#125;  &#125;  void operator=(BigInt x) &#123;    Init();    n = x.n;    f = x.f;    for (int i = 0; i &lt; n; i++) &#123;      a[i] = x[i];    &#125;  &#125;  bool operator==(BigInt x) &#123;    if (n != x.n) &#123;      return 0;    &#125;    for (int i = n - 1; i &gt;= 0; i--) &#123;      if (a[i] != x[i]) &#123;        return 0;      &#125;    &#125;    return 1;  &#125;  bool operator!=(BigInt x) &#123; return !operator==(x); &#125;  bool operator&lt;(BigInt x) &#123;    if (n == x.n) &#123;      for (int i = n - 1; i &gt;= 0; i--) &#123;        if (a[i] != x[i]) &#123;          return a[i] &lt; x[i];        &#125;      &#125;    &#125;    return n &lt; x.n;  &#125;  bool operator&gt;(BigInt x) &#123;    if (n == x.n) &#123;      for (int i = n - 1; i &gt;= 0; i--) &#123;        if (a[i] != x[i]) &#123;          return a[i] &gt; x[i];        &#125;      &#125;    &#125;    return n &gt; x.n;  &#125;  bool operator&lt;=(BigInt x) &#123; return !operator&gt;(x); &#125;  bool operator&gt;=(BigInt x) &#123; return !operator&lt;(x); &#125;  bool operator!() &#123;    if (n != 1) &#123;      return 0;    &#125;    return !a[0];  &#125;  bool operator==(int x) &#123;    BigInt y;    y = x;    return operator==(y);  &#125;  bool operator!=(int x) &#123;    BigInt y;    y = x;    return operator!=(y);  &#125;  bool operator&lt;(int x) &#123;    BigInt y;    y = x;    return operator&lt;(y);  &#125;  bool operator&gt;(int x) &#123;    BigInt y;    y = x;    return operator&gt;(y);  &#125;  bool operator&lt;=(int x) &#123;    BigInt y;    y = x;    return operator&lt;=(y);  &#125;  bool operator&gt;=(int x) &#123;    BigInt y;    y = x;    return operator&gt;=(y);  &#125;  void ChangeBinary(BigInt &amp;num) &#123;    BigInt ans;    ans[0] = 0, ans.n = 1;    for (int i = num.n - 1; i &gt;= 0; i--) &#123;      ans = ans * binary;      ans = ans + num[i];    &#125;    num = ans;  &#125;  BigInt operator+(BigInt x) &#123;    BigInt y;    y.n = std::max(n, x.n);    int s = 0;    for (int i = 0; i &lt; y.n; i++) &#123;      s += a[i] + x.a[i];      y.a[i] = s % binary;      s /= binary;    &#125;    for (; s; s /= binary) &#123;      y.a[y.n++] += s % binary;    &#125;    return y;  &#125;  void operator+=(BigInt x) &#123;    BigInt z = *this;    *this = z + x;  &#125;  void operator+=(int x) &#123;    BigInt z = *this;    *this = z + x;  &#125;  BigInt operator+(int x) &#123;    BigInt y;    y = x;    return operator+(y);  &#125;  BigInt operator-(BigInt x) &#123;    BigInt y, z = *this;    if (z &lt; x) &#123;      y.f = 0, std::swap(z, x);    &#125;    y.n = z.n;    for (int i = 0; i &lt; y.n; i++) &#123;      if (z.a[i] &lt; x[i]) &#123;        z[i + 1]--;        z[i] += binary;      &#125;      y[i] = z[i] - x[i];    &#125;    for (; !y[y.n - 1] &amp;&amp; y.n &gt; 1; y.n--) &#123;    &#125;    return y;  &#125;  void operator-=(BigInt x) &#123;    BigInt z = *this;    *this = z - x;  &#125;  void operator-=(int x) &#123;    BigInt z = *this;    *this = z - x;  &#125;  BigInt operator-(int x) &#123;    BigInt y;    y = x;    return operator-(y);  &#125;  BigInt operator*(BigInt x) &#123;    BigInt y;    for (int i = 0; i &lt; n; i++) &#123;      for (int j = 0; j &lt; x.n; j++) &#123;        y[i + j] += a[i] * x[j];      &#125;    &#125;    y.n = n + x.n;    for (int i = 0; i &lt; y.n - 1; i++) &#123;      y[i + 1] += y[i] / binary, y[i] %= binary;    &#125;    for (; !y.a[y.n - 1] &amp;&amp; y.n &gt; 1; y.n--) &#123;    &#125;    return y;  &#125;  void operator*=(BigInt x) &#123;    BigInt z = *this;    *this = z * x;  &#125;  void operator*=(int x) &#123;    BigInt z = *this;    *this = z * x;  &#125;  BigInt operator*(int x) &#123;    BigInt y;    y = x;    return operator*(y);  &#125;  BigInt operator/(BigInt x) &#123;    BigInt y, z = *this;    if (x == y) &#123;      return x;    &#125;    if (binary != 10) &#123;      std::cout &lt;&lt; x &lt;&lt; &#x27; &#x27; &lt;&lt; z &lt;&lt; &#x27;\n&#x27;;      ChangeBinary(x), ChangeBinary(z);      std::cout &lt;&lt; x &lt;&lt; &#x27; &#x27; &lt;&lt; z &lt;&lt; &#x27;\n&#x27;;    &#125;    y.n = z.n - x.n + 1;    for (int i = y.n - 1; i &gt;= 0; i--) &#123;      BigInt t = x &lt;&lt; i;      for (; z &gt;= t; z -= t) &#123;        y.a[i]++;      &#125;    &#125;    for (; !y.a[y.n - 1] &amp;&amp; y.n &gt; 1; y.n--) &#123;    &#125;    return y;  &#125;  void operator/=(BigInt x) &#123;    BigInt z = *this;    *this = z / x;  &#125;  void operator/=(int x) &#123;    BigInt z = *this;    *this = z / x;  &#125;  BigInt operator/(int x) &#123;    BigInt y;    y = x;    return operator/(y);  &#125;  BigInt operator%(BigInt x) &#123;    BigInt z = *this;    return z - z / x * x;  &#125;  void operator%=(BigInt x) &#123;    BigInt z = *this;    *this = z % x;  &#125;  void operator%=(int x) &#123;    BigInt z = *this;    *this = z % x;  &#125;  BigInt operator%(int x) &#123;    BigInt y;    y = x;    return operator%(y);  &#125;  BigInt operator&lt;&lt;(int l) &#123;    BigInt x;    for (int i = 0; i &lt; n; i++) &#123;      x[i + l] = a[i];    &#125;    x.n = n + l;    return x;  &#125;  BigInt operator&gt;&gt;(int l) &#123;    BigInt x;    x.n = n - l;    for (int i = 0; i &lt; x.n; i++) &#123;      x[i] = a[i + l];    &#125;    return x;  &#125;&#125;;&#125;; // namespace Integer// upd 2023.12.11 出现了一些 bug，已修正。
]]></content>
  </entry>
  <entry>
    <title>堆</title>
    <url>/2024/02/06/heap/</url>
    <content><![CDATA[堆是一种支持动态快速查询最值的数据结构，支持以下操作：

高效插入元素。
高效查询、删除最值。
高效修改指定元素、删除指定元素。

性质平时说的堆通常就指二叉堆,二叉堆是一种完全二叉树,它具有下面的性质:

如果堆为小根堆

父节点的值不大于两个儿子的值

如果堆为大根堆

父结点的值不小于两个儿子的值


堆的根结点⼜被称为堆顶，⼩根堆的堆顶元素是堆中最⼩的元素，大根堆就反着来。
存储因为它是完全二叉树，所以可以直接用数组存储：
int heap[MAXN], len;
基本操作一个容器，最先要考虑的是增、删、改、查四个操作。
当对堆进行上面的操作的时候，可能会破坏堆的性质，也可能让堆不是完全二叉树，要考虑如何处理。
可以用两种操作来维护堆的性质：上浮、下沉，仅适用于小根堆，大根堆两个操作相反。
上浮当修改了堆中的某一个元素的时候，如果这个元素变小了：

如果这个结点有父亲结点，并且值比父亲更小，那么交换当前结点与父亲节点的值，继续执行操作。
否则，停止操作。

实现void up(int x) { // 上浮  for (int i = x, j = i / 2; j &gt;= 1 &amp;&amp; heap[i] &lt; heap[j]; i = j, j = i / 2) {    swap(heap[i], heap[j]);  }}
下沉当修改了堆中的某个元素的时候，如果这个元素变大了：

如果这个结点有儿子结点，并且值比较小的儿子更大，那么交换当前结点与较小的儿子结点的值，继续执行操作

实现int son(int x) { // 返回较小的儿子  int i = x * 2, j = x * 2 + 1;  return i + (j &lt;= n &amp;&amp; heap[i] &gt; heap[j]);}void down(int x) { // 下沉  for (int i = x, j = son(i); j &lt;= n &amp;&amp; heap[i] &gt; heap[j]; i = j, j = son(i)) {    swap(heap[i], heap[j]);  }}
查询最值int top() { // 查询最值  return heap[1]; // 小根堆则为最小值，大根堆则为最大值}
插入元素可以在堆的尾部增加一个结点，保证堆仍然是一棵完全二叉树，然后上浮。
void push(int x) {  heap[++n] = x;  up(n);}
删除最值如果直接删除堆顶，会使堆分裂为两个堆，⽽删除堆尾元素不需要维护。可以将堆顶和堆尾元素进⾏交换，然后删除堆尾元素，保证堆仍然是⼀棵完全⼆叉树。 如果交换后的堆顶不满⾜堆的性质，就下沉。
void pop() {  swap(heap[1], heap[n--]);  down(1);}
priority_queuepriority_queue，即优先队列，就是 C++ 已经弄好的堆，比赛时常用这个容器代替堆。
头文件#include &lt;queue&gt;
定义#include &lt;queue&gt;#include &lt;vector&gt;#include &lt;functional&gt;// 需要 3 个参数：Typename数据类型名，Container底层容器，Compare比较器// 底层容器和比较器为可选参数// 不指定底层容器和比较器时，默认为 vector&lt;Typename&gt; 和 less&lt;Typename&gt;（也就是大根堆）priority_queue&lt;Typename, [Container], [Compare])&gt; q;// 大根堆priority_queue&lt;int&gt; q1;// 定义了一个类型为 int，底层容器为 vector&lt;int&gt;，比较器为 less&lt;int&gt; 的优先队列// 小根堆priority_queue&lt;int, vector&lt;int&gt;, greater&lt;int&gt;&gt; q2;// 定义了一个类型为 int，底层容器为 vector&lt;int&gt;，比较器为 greater&lt;int&gt; 的优先队列// 需要引入头文件 &lt;functional&gt;// less 中文为较小的，但实际上是大数靠前的比较器，greater 为较大的，实际上是小数靠前的比较器。
成员函数 为堆内元素个数。

top() 访问堆顶，
empty() 判断是否为空，
size() 元素个数，
push(x) 插入 ，
pop() 弹出堆顶，
注意：没有 clear() 函数，清空需要不断 pop()

自定义比较器对于结构体类型，我们需要定义其⽐较规则才能⽤优先队列维护该类型元素。
struct Node {	int x, id;};
方法 1：定义比较器函数对象struct cmp {	bool operator()(const Node &amp;i, const Node &amp;j) const { // 函数尾的 const 表示该函数不能对所在类的成员进⾏修改		return i.x &lt; j.x;	}};priority_queue&lt;Node, vector&lt;Node&gt;, cmp&gt; pq; // 定义了⼀个 x 为关键字的⼤根堆
方法 2：重载运算符bool operator&lt;(const Node &amp;i, const Node &amp;j) {	return i.x &gt; j.x;}priority_queue&lt;Node&gt; pq; // 定义了⼀个 x 为关键字的⼩根堆
struct Node {  int x, id;  bool operator &lt; (const Node &amp;i) const {	  return x &gt; i.x;  }};priority_queue&lt;Node&gt; pq; // 定义了⼀个 x 为关键字的⼩根堆
方法 3：使⽤ lambda 表达式（需要 C++11 及以上标准）auto cmp = [](const Node &amp;i, const Node &amp;j) {	return i.x &lt; j.x;};priority_queue&lt;Node, vector&lt;Node&gt;, decltype(cmp)) pq; // x 为关键字的⼤根堆
例子struct cmp{  bool operator()(int i, int j) {    return i &gt; j;  }};priority_queue&lt;int&gt; pq1; // ⼤根堆priority_queue&lt;int, vector&lt;int&gt;, cmp&gt; pq2; // 小根堆pq1.push(1);pq1.push(2);cout &lt;&lt; pq1.top() &lt;&lt; '\n'; // 2pq1.pop();cout &lt;&lt; pq1.top() &lt;&lt; '\n'; // 1pq2.push(1);pq2.push(2);cout &lt;&lt; pq2.top() &lt;&lt; '\n'; // 1pq2.pop();cout &lt;&lt; pq2.top() &lt;&lt; '\n'; // 2
结构体struct Student {	int a, b, c; // 三科的成绩};struct cmp {  bool operator()(const Student &amp;i, const Student &amp;j) {    if (i.a != j.a) {      return i.a &lt; j.a;    }    if (i.b != j.b) {      return i.b &lt; j.b;    }    return i.c &lt; j.c;  }};priority_queue&lt;Student, vector&lt;Student&gt;, cmp&gt; pq; // 按语数外从⾼到低排int main() {  ios::sync_with_stdio(0);  cin.tie(0), cout.tie(0);  pq.push({100, 100, 90});  pq.push({100, 100, 100});  pq.push({100, 90, 100});  pq.push({90, 100, 100});  while (!pq.empty()) {    Student x = pq.top();    pq.pop();    cout &lt;&lt; x.a &lt;&lt; ' ' &lt;&lt; x.b &lt;&lt; ' ' &lt;&lt; x.c &lt;&lt; '\n';  }  return 0;}
例题P1090 [NOIP2004 提高组] 合并果子贪⼼，每次选出数⽬最⼩的两堆进⾏合并即可。暴⼒模拟是 ，可以⽤优先队列优化⾄ 。如果把合并过程建树，那么题⽬所要求的的答案就是 ， 为数⽬， 为 在树中的深度，显然  ⼤的叶⼦结点  ⼩的两堆进⾏合并。尽可能⼩，因此每次选出数⽬最⼩的两堆进⾏合并。
#include &lt;bits/stdc++.h&gt;using namespace std;int n, x;priority_queue&lt;int, vector&lt;int&gt;, greater&lt;int&gt;&gt; pq;int main() {  cin &gt;&gt; n;  for (int i = 1; i &lt;= n; i++) {    cin &gt;&gt; x;    pq.push(x);  }  int ans = 0;  while (pq.size() != 1) {    int x = pq.top();    pq.pop();    int y = pq.top();    pq.pop();    ans += x + y;    pq.push(x + y);  }  cout &lt;&lt; ans;  return 0;}
P1631 序列合并堆优化。
如果暴⼒记录所有和，再加上排序，时间复杂度⾼达 。
我们可以先对  个和进⾏分组：

第⼀组：
第⼆组：
第  组：

由于题⽬给出的序列  和  都是单调不减的，我们发现每组内的  个和也都是单调不减的。可以暴⼒⽤  个指针维护每⼀组的最⼩值，每次取⼀个最⼩值 ，总时间 。
设计状态【和，和所属的组，和在所属组中是第⼏个】。⼀开始先将每组的最⼩和的状态存储在堆中。每次取出堆中的最⼩和后，将该组的后⼀个和压⼊到堆中。执⾏ 次这样的操作，最⼩的  个和就得到了。总时间 。为了⽅便实现，使⽤结构体表示状态，使⽤ priority_queue 代替堆。
#include &lt;bits/stdc++.h&gt;using namespace std;const int MAXN = 1e5 + 10;struct node {  int sum, i, j;  bool operator&lt;(const node &amp;j) const {    return sum &gt; j.sum;  }};int n, a[MAXN], b[MAXN];priority_queue&lt;node&gt; pq;int main() {  cin &gt;&gt; n;  for (int i = 1; i &lt;= n; i++) {    cin &gt;&gt; a[i];  }  for (int i = 1; i &lt;= n; i++) {    cin &gt;&gt; b[i];  }  for (int i = 1; i &lt;= n; i++) {    pq.push({a[i] + b[1], i, 1});  }  for (int i = 1; i &lt;= n; i++) {    cout &lt;&lt; pq.top().sum &lt;&lt; ' ';    node now = pq.top();    pq.pop();    pq.push({a[now.i] + b[now.j + 1], now.i, now.j + 1});  }  return 0;}
P4053 [JSOI2007] 建筑抢修这个题状态有两个属性【修理时⻓，截⽌时刻】，可以先对截⽌时刻排序（相当于是给所有建筑定义了⼀个优先级，截⽌时间越早的肯定越要先完成）。
按排序后的顺序模拟修理建筑，并记录修理总时⻓。反例如下：
3100 1002 1013 102按此贪⼼只能修第 1 个建筑，实际能修第 2、3 个 建筑
如果总时⻓不超过当前建筑的截⽌时刻，那就把当前建筑加⼊到修理计划当中；
否则，说明必然有⼀个建筑是不能修理的，为了让后⾯的建筑修理有更多的时间，我们应该不去修理前⾯所有建筑中时⻓最⻓的那⼀个。
可以⽤堆来存储所有修理建筑的时⻓，加⼊修理计划——堆中插⼊元素，移除时⻓最⻓的建筑——删除堆中最⼤值。
#include &lt;bits/stdc++.h&gt;using namespace std;const int MAXN = 15e4 + 10;struct node {  int t1, t2;} a[MAXN];int n;priority_queue&lt;int&gt; pq;bool cmp(const node &amp;i, const node &amp;j) {  return i.t2 &lt; j.t2;}int main() {  cin &gt;&gt; n;  for (int i = 1; i &lt;= n; i++) {    cin &gt;&gt; a[i].t1 &gt;&gt; a[i].t2;  }  sort(a + 1, a + n + 1, cmp);  long long sum = 0, ans = 0;  for (int i = 1; i &lt;= n; i++) {    sum += a[i].t1;    pq.push(a[i].t1);    if (sum &lt;= a[i].t2) {      ans++;    } else {      sum -= pq.top();      pq.pop();    }  }  cout &lt;&lt; ans;  return 0;}
]]></content>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2025/09/22/hello-world/</url>
    <content><![CDATA[Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub.
Quick StartCreate a new post$ hexo new "My New Post"
More info: Writing
Run server$ hexo server
More info: Server
Generate static files$ hexo generate
More info: Generating
Deploy to remote sites$ hexo deploy
More info: Deployment
$dpi = dp{i - 1} + a_i$
]]></content>
  </entry>
  <entry>
    <title>【ABC105D】题解</title>
    <url>/2024/02/16/%E3%80%90ABC105D%E3%80%91%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[题解题意简述
给定  个数，求这  个数中有多少个二元组  满足其中每一个数都是  的倍数。

思路前缀和， 内每一个数 ，可以用 $(sumy - sum{x - 1}) \bmod \ m = 0$ 表示。但是这题数据太大，所以要使用 map。
如果 ，那么 ，所以要先给  赋值为 
AC Code#include&lt;bits/stdc++.h&gt;using namespace std;int n, m, x;map&lt;long long, int&gt; cnt;long long sum, ans;int main(){  cin &gt;&gt; n &gt;&gt; m;  cnt[0] = 1;  for (int i = 1; i &lt;= n; i++){    cin &gt;&gt; x;    sum += x;    ans += cnt[sum % m];    cnt[sum % m]++;  }  cout &lt;&lt; ans;  return 0;}
]]></content>
      <categories>
        <category>芝士</category>
      </categories>
      <tags>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>题解：K-th Largest Connected Components</title>
    <url>/2024/09/21/%E9%A2%98%E8%A7%A3%EF%BC%9AK-th%20Largest%20Connected%20Components/</url>
    <content><![CDATA[题意给定  个点  条边的无向图和  次操作，每次操作分为两种：

1 u v 表示在  之间建立一条边。
2 v k 表示查询能够到达  的点中第  大的顶点编号。

思路并查集好题，这个笨比赛时没想到。
题目要求的是能够到达的点中第  大的顶点编号，而不是有直接连边的点。直接对每个点记录连出去的边然后手写堆查找肯定是不行的，这时可以考虑把所有点收在若干棵树内，这就叫并查集。
此时在同一棵树内的点就是相互可达的点，此时就可以做了。但是你会发现一些问题：

每次查询需要在一整棵树内找第  大，TLE。
每个点存储了太多子结结点的信息，MLE。

实际上，每次合并可以把两棵树的结点按照从大到小的顺序存储到同一个结点内，这样就省下了每次查询的时间，由于 ，所以每个点不需要存储所有的可达的点，只需要存储编号最大的  个即可，这样就省下了空间。
代码#include &lt;bits/stdc++.h&gt;using namespace std;const int MAXN = 2e5 + 10;int fa[MAXN];vector&lt;int&gt; a[MAXN];int n, q;int find(int x) {  return fa[x] == x ? x : fa[x] = find(fa[x]);}void merge(int u, int v) {  int fu = find(u), fv = find(v);  if (fu == fv) {    return;  }  vector&lt;int&gt; x;  for (int i : a[fu]) {    x.push_back(i);  }  for (int i : a[fv]) {    x.push_back(i);  }  sort(x.begin(), x.end(), greater&lt;int&gt;());  a[fu].clear();  a[fv].clear();  for (int i = 0; i &lt; min(10, int(x.size())); i++) {    a[fv].push_back(x[i]);  }  fa[fu] = fv;}int main() {  ios::sync_with_stdio(0), cin.tie(0);  cin &gt;&gt; n &gt;&gt; q;  for (int i = 1; i &lt;= n; i++) {    fa[i] = i;    a[i].push_back(i);  }  for (int op, x, y; q; q--) {    cin &gt;&gt; op &gt;&gt; x &gt;&gt; y;    if (op == 1) {      merge(x, y);    } else {      x = find(x);      cout &lt;&lt; (y &gt; a[x].size() ? -1 : a[x][y - 1]) &lt;&lt; '\n';    }  }  return 0;}
]]></content>
  </entry>
</search>
